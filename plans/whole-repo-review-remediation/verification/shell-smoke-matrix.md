# Shell Smoke Matrix

Plan: `whole-repo-review-remediation`

This matrix captures the minimum shell-facing smoke scenarios required by the remediation plan. Populate status as phases complete.

| Phase | Shell | Scenario | Expected Result | Status | Evidence / Notes |
|-------|-------|----------|-----------------|--------|------------------|
| 2 | Bash | `dx menu --cwd <path>` uses explicit cwd for `paths` mode | Candidates reflect `<path>`, not process cwd | Pass | Process cwd set to empty temp dir; explicit cwd had `alpha/`. `DX_MENU_DEBUG=1 ... menu --buffer "cd a" --cursor 4 --cwd <explicit_dir>` logged `mode=Paths query=Some("a") replace=[3,4)`, `candidates=1`, and replace value ending `<explicit_dir>/alpha/`. |
| 2 | Zsh | `dx menu --cwd <path>` uses explicit cwd for `paths` mode | Candidates reflect `<path>`, not process cwd | Pass | Mirrored Bash smoke via `zsh -lc` with empty process cwd and explicit cwd containing `alpha/`; replace action value ended `<explicit_dir>/alpha/`. |
| 2 | Fish | `dx menu --cwd <path>` paths behavior | Pass or `Not Feasible` with reason | Not Feasible | `command -v fish` returned no path in this environment, so Fish shell smoke could not be executed. |
| 2 | PowerShell | `dx menu --cwd <path>` / menu fallback behavior | Candidates or fallback reflect explicit cwd contract | Pass | `pwsh` smoke ran from empty process cwd with explicit cwd containing `alpha/`; debug showed `mode=Paths query=Some("a") replace=[3,4)` and replace value ending `<explicit_dir>/alpha/`, satisfying explicit cwd contract. |
| 2 | Bash/Zsh | Approved flagged `cd` forms (`-L`, `-P`, `--`) | Menu replacement span starts at path token only (flags preserved); `cd -` and unsupported/grouped flags fall back to native completion behavior | Pass | In Bash and Zsh, `menu --buffer "cd -P f" --cursor 7 --cwd <explicit_dir>` returned `replaceStart=6`, `replaceEnd=7`, value ending `<explicit_dir>/foo/`; `menu --buffer "cd -" --cursor 4` logged `parse_buffer returned None -> noop`. |
| 2 | PowerShell | POSIX flagged `cd` forms under `--psreadline-mode` (`-L`, `-P`, `--`) | Non-intervention: returns noop/fallback path (`TabCompleteNext`), with no PowerShell-specific grammar reinterpretation | Pass | `pwsh ... menu --buffer "cd -P foo" --cursor 9 --psreadline-mode` logged `parse_buffer returned None -> noop` then `{"action":"noop"}`, confirming non-intervention fallback in PSReadLine mode. |
| 3 | Bash | Generated-hook guard coverage replaces legacy prototype path | Equivalent guard behavior validated without `scripts/hooks/*` | Pass | Exact runtime tests passed in `tests/shell_hook_guard.rs`: `bash_generated_hook_command_not_found_guard_prevents_recursive_resolve_calls`, `bash_generated_hook_command_not_found_resolves_path_like_command_once`, and `bash_generated_hook_cd_wrapper_invokes_dx_once_and_changes_directory`. Legacy prototype file `scripts/hooks/dx.bash` is deleted. |
| 3 | Zsh | Legacy prototype retirement remains safe | Generated-hook tests and/or smoke evidence justify deletion | Pass | `command -v zsh` resolved to `/etc/profiles/per-user/nick/bin/zsh`; exact runtime tests passed in `tests/shell_hook_guard.rs`: `zsh_generated_hook_command_not_found_guard_prevents_recursive_resolve_calls` and `zsh_generated_hook_command_not_found_resolves_path_like_command_once`; supporting generated-hook contract test passed (`cargo test --lib hooks::tests::all_shells_freeze_command_not_found_guard_contract_markers -- --exact`). Legacy prototype `scripts/hooks/dx.zsh` is deleted. |
| 4 | Bash | Final generated init + menu fallback contract | Generated-init contract check confirms `dx menu --buffer` and explicit fallback path; non-interactive `dx menu` returns `{"action":"noop"}` | Pass | Evidence is generated-init contract markers (`__dx_try_menu`, `dx menu --buffer`, `if __dx_try_menu; then`, `return 0`, `</dev/tty`) plus non-interactive noop output (`menu_noop={"action":"noop"}`); no separate interactive Bash runtime transcript was captured in this row. |
| 4 | Zsh | Final generated init + menu fallback contract | Generated-init contract check confirms menu widget with `expand-or-complete` fallback branches; non-interactive `dx menu` returns `{"action":"noop"}` | Pass | Evidence is generated-init contract markers (`__dx_menu_widget`, `bindkey '^I' __dx_menu_widget`, `dx menu --buffer`, `zle expand-or-complete`, `</dev/tty`) plus non-interactive noop output (`menu_noop={"action":"noop"}`); no separate interactive Zsh runtime transcript was captured in this row. |
| 4 | Fish | Final generated init + menu fallback contract | Generated-init contract check confirms `commandline -f complete` fallback path; non-interactive `dx menu` returns `{"action":"noop"}` | Pass | Evidence is generated-init contract markers (`function __dx_menu_complete`, `bind \t __dx_menu_complete`, `dx menu --buffer`, `commandline -f complete`, `</dev/tty`) plus non-interactive noop output (`menu_noop={"action":"noop"}`); no live Fish runtime execution evidence was captured in this environment for this row. |
| 4 | PowerShell | `Invoke-Expression ((& dx init pwsh | Out-String))` + menu fallback contract | Hook evaluates as one script block, menu handler preserves `ConvertFrom-Json` parsing and falls back via `TabCompleteNext` on noop/error | Pass | One-script-block evaluation used `Invoke-Expression ((& dx init pwsh --menu | Out-String))` and returned exactly `Handler=True`, `ConvertFromJson=True`, `TabCompleteNext=True`, `Menu={"action":"noop"}`; generated marker check also confirmed `--psreadline-mode`, `Set-PSReadLineKeyHandler -Key Tab`, `ConvertFrom-Json`, and `TabCompleteNext`. |
| 4 | PowerShell | `ProxyCommand` decision evidence (only if adopted) | New wrapper path passes required behavior matrix | Not Applicable | ProxyCommand path was evaluated and rejected in Phase 4 (no net correctness win; unsupported-flag behavior risk). See `verification/proxycommand-eval-phase-4.md`. |
